node_modules/
# dist/ liegt bewusst im Repo: Plesk deployt per `git pull` und baut selbst
# nichts. Auf main committet die Pipeline den Build selbst hinterher, auf
# Feature-Branches bleibt dist/ absichtlich unangetastet (siehe README).
.DS_Store
*.local
.vite/
