{
  "name": "mundwerk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "fixtures": "node scripts/make-fixtures.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
